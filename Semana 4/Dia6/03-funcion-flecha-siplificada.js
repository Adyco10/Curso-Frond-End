// si la funcion solo tiene  uan linea de ejecucion se puede omitir las llaves
//se peuden omitir las llaves "{}"
const sumar = (a, b) => console.log(a + b);
sumar(52, 11);
// si la cundion reciibe un paramtero se puede omitr  los parentesis
const cuadrado = (numero) => console.log(numero * numero);
cuadrado(12);
//si a funcion solo tiene una linea d eejcucion y retorna un valor
 // se puede obviar la palabara resrvada return y en su lugar
 //encerrar erl retorno en un parentesis
 const cubo = n => ( n*n*n);
 console.log(cubo(9));